# Optional cross-device sync via Upstash Redis (free tier).
# Leave blank to run as a single-browser app (localStorage only).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional shared password to gate reads/writes.
# Leave blank to allow open access.
APP_PASSWORD=
